"""Hosted (model_api) user line: config, chat, history import, wake consumer.

HTTP surface is native ASGI (hosted.*_asgi, wired by asgi_app); the Flask
blueprints were deleted in the ASGI cutover."""
